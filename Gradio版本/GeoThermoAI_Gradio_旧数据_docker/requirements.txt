# GeoThermoAI Docker 版依赖
# 运行环境：ghcr.io/osgeo/gdal:ubuntu-small-3.9.3（Ubuntu 24.04 / Python 3.12 / GDAL + osgeo 已预装）
# 注意：GDAL/osgeo 由镜像自带（osgeo 绑定按 apt numpy 1.x 编译），这里不装任何 GDAL 相关包。

# UI 框架（pip 会自动选择兼容 Python 3.12 的最新 gradio 6.x）
gradio>=6.0.0
# AI / LLM
requests>=2.28
# 遥感数据处理（rasterio / geopandas 均走自带 GDAL 的 wheel，与镜像 osgeo 互不干扰）
rasterio>=1.3
# numpy<2 必须锁住：镜像 osgeo 是按 apt numpy 1.x 编译的，升到 2.x 会导致 import osgeo 失败
numpy>=1.23,<2
scipy>=1.9
# 地图可视化
folium>=0.16
matplotlib>=3.7.0
# 机器学习
scikit-learn>=1.0
joblib>=1.2
# 数据处理
pandas>=1.5
geopandas>=0.12
shapely>=2.0
pyshp>=2.3
# STAC 数据源
pystac-client>=0.7.0
planetary-computer>=1.0.0
