1. ~~用户选择了什么影像也应当存入记忆，假如现在有多对影像，用户选择了其中的一对，那么Agent应当记住用户选择了哪一对影像，假如用户在训练时因为精度不好，Agent提示可以选择其他影像时，之前选择的那对影像不能再成为推荐影像，而之前选择的影像也需要被标注为已选择过，即使用户选择新影像后再次回退到影像选择步骤，依然如此~~
2. ~~同一个项目中，每对影像的文件夹是独立的（这个文件夹包括影像及这对影像后续处理产生的所有文件）~~
3. ~~假如检索到多对影像，用户选择了一对，这一对已经下载好了，如果用户后面再一次选择这对影像的时候，不需要重复下载，直接从本地文件夹中读取即可；假如用户选择了其他对影像，但是这对影像里面的部分影像和前一对是重复的时候，重复的那几景影像无需重复下载，直接从前一对的影像的文件夹复制到这对影像的文件夹即可；另外DEM也是不需要重复下载的~~
4. ~~本地储存的LST影像和sentinel-2影像的文件名都需要带上日期，格式为YYYYMMDD，下载的30m的LST的日期为影像日期，下载的sentinel-2影像的日期为影像日期，生成的10m的LST的日期和sentinel-2影像的日期相同，DEM不需要日期，因此你的前端地图界面显示影像时也要显示日期（除了DEM）~~
5. ~~当前项目在进行时，如果用户新建一个项目或者是切换到已存在的另外的项目，不管有没有切换回之前的项目，之前的项目都不能被打断~~
6. ~~前端中除了API、数据源、测试三个页面可以每个项目都是一样，对话界面和工作面板其他页面都需要根据当前所处的项目来显示不同的内容。当前软件的情况是是必须进入新项目的新对话时，对话页面才会清空，但是工作面板各页面还是之前项目的~~
7. ~~当用户重新选择数据下载，或者是回到了已经完成的某个步骤时，再往下进行时，工作面板的进度需要保持一致，总之进度需要和现在的实际情况保持一致~~
8. ~~由我批准模式下，全流程跑完时也必须向用户提问，是否接受结果，用户需要确认后才会接受结果，提问用弹窗，和现在选择影像、回答问题时的弹窗风格是一致的，精度页面改成最终精度，用于选择用户确认接受结果后的精度，最终精度页面也要随对话动态更新，假如用户切换到另外一个对话（无论是不是同一个项目，最终精度页面都需要随着更新）~~
9. ~~测试页面不应该只有微软行星计算机和GDAL，你目前的Copernicus相关的也需要~~
10. ~~不要在前端显示”\[规则]D7 判定不合格”类似的字眼，比如由我批准模式下，数据处理未通过提示用户改做出选择时~~
11. ~~Agent向我提问时，不要有更换数据源这个功能和选项~~
12. ~~Agent应该聪明一点，在由我批准模式下，假如我现在已经把每一对影像都尝试了都没有拿去跑后续的流程去出结果，那么Agent就不应该提示我换一对影像了~~
13. ~~切到了一个项目的时候，对话页面也需要更新呀，如果这个项目没有对话，也不能显示之前的对话内容啊~~
14. ~~当前气泡在生成时，第一行总是有一个粗的黑竖线，第二行是闪烁的光标，我现在不需要这个黑竖线，光标保留~~
15. ~~模仿DeepSeek对话网页，在气泡上方用可折叠的形式展示LLM的思考过程~~
16. ~~思考过程需要写明用时多少秒，格式为(用时XX秒)，要像我这个格式一样使用括号~~
17. ~~我需要升级为两种模式，一种模式叫Chat，一种模式叫Work，两种模式切换按钮风格参考目前"由我批准"和"完全执行"切换按钮，Chat模式和Work模式的切换按钮放在当前对话主页面顶端显示对话名称的左边（因此目前对话名称需要向右移动一点点），Chat模式下，用户只能对话，Agent对当前已有的文件只能是已读模式，不能生成或者是修改文件，也不能执行我的降尺度工作流，Work模式则没有这些限制。只有Work模式下，输入框才需要显示目前的"由我批准"和"完全执行"切换按钮，Chat模式不显示~~
18. ~~当前图层渲染存在这个问题：假如我的底图是街道地图，我也勾选了图层显示，但是当我切换到别的底图时，图层就显示不了，必须等我点刷新才能显示，我需要底图切换不影响图层显示~~
19. ~~思考时默认不展开思考过程，思考过程内容需要用户点击才能展开~~
20. ~~假如用户的研究区和地图不是同一个地区，当有图层需要渲染时，会不会自动把地图居中到图层区域，如果没有需要这样改进~~
21. ~~工作面板里面的地图页面的图层控制页面应该更宽一点，尽量展示完整每个图层的名称或者是把名称展示得更多一点~~
22. ~~工作面板的API设置、数据源、参数页面的保存按钮，都要加上保存图标，且要和工作面板各页面的图标风格一致（假如现在已有图标但是风格不一样，需要替换）~~
23. ~~工作面板的测试页面的三个测试按钮都使用白色底色，三个按钮的图标都要和测试页面的图标一模一样（此处是直接用一样的图标）；另外每次测试时，测试内容的显示风格ai味好重，而且和整个软件的整体风格不一致，需要优化~~
24. ~~删除项目或者是对话时，删除时间太长，需要等很久才有删除成功的提示，需要优化删除速度~~
25. ~~工作面板的测试页面的三个测试按钮都使用蓝底白字；当前测试通过后的最后一行字总是会有一条横着的短虚线，删除这个虚线；测试通过显示的内容，对勾的图标能不能换一个，感觉现在这个的ai味好重~~
26. ~~下载功能不能一次只能下载一个文件，要支持多文件可选下载~~
27. ~~研究区 鄂州市\_市，时间 2024 年 7 月，云量上限 30。共 7 步：数据获取、数据预处理、地形热响应指数计算、模型训练、热约束残差计算、地表温度产品导出、闭合精度校核。这个弹窗里面这段文字要换行显示， 从共7步这一句换行，前后两句都不要有句号~~
28. ~~下载页面，不要在影像一开始被下载的时候就显示在下载列表，其他文件也是，必需等文件生成完整了才显示在下载列表；选中文件弹出的下载按钮的图标也不是和整体风格一致的线性 SVG 图标，要换成这个~~
29. ~~地图瓦片的加载渲染速度还不够快~~
30. ~~结果后处理，无论是气泡输出还是日志，都只是告诉了用户开始但是没有告诉用户结束，只有工作面板的进度告诉了用户已完成，地图也是正常显示了，但是地图页面不要叫“填洞后”，要叫“无空洞”~~
31. ~~结果后处理算法有问题，怎么把几景影像覆盖的全部区域都填充了，也感觉像是把研究区bbox范围都填充了，这不对，我只需要研究区矢量范围内的，无论是你的算法计算过程还是最终填充的结果，都只需要研究区矢量范围内的~~
32. ~~我怎么感觉当我说“根据现有结果生成无空洞数据”，没有走LLM，立马就给我弹窗了，我需要让LLM理解我说的话，然后去项目目录确认是否真的存在，再弹窗，本质上是个LLM思考分析的过程，但是我现在感觉像规则判断~~
33. ~~模型训练部分的气泡输出还可以优化。“模型训练完成：测试集决定系数 0.81，均方根误差 2.32 K”改为“模型训练（第一轮）完成：测试集决定系数 0.81，均方根误差 2.32 K”，后一句“第 1 轮训练完成：测试集决定系数 0.81，均方根误差 2.32 K”可以删除；“开始第 2 轮调优训练”“训练测试差距小，可增树并减叶节点样本以提升精度”这两句居然都没有缩进，需要缩进，但是不需要•，另外把“开始第 2 轮调优训练”改为“开始调优训练（第2轮）”，假如还需要更多次调优，每次都以此类推；“第 2 轮训练完成：测试集决定系数 0.81，均方根误差 2.32 K”也需要删除，因为每次训练都已经报告了一次训练完成和精度，后续调优每一轮之间都用分割线隔开即可；“调优结束（共 2 轮），采用第 2 轮的结果：测试集决定系数 0.81，均方根误差 2.32 K”，这句话也没有缩进，需要缩进但不需要•；其余步骤没有缩进的文字部分，你也需要仔细审查，做出合理的排版优化，比如“数据检查通过，进入模型训练”也应该是缩进不带•~~
34. ~~结果说明部分的气泡应该是一整个部分啊，只需要结果说明那里有蓝色的竖线，该部分其他地方不应该有（比如产品概况、模型精度、闭合情况等），而是用多级标题和缩进来区分层级~~
35. ~~“（本次为完全执行模式，默认不执行结果后处理；如需要无空洞的 10m 地表温度产品，告诉我即可对结果做空洞填补。）”这句话的缩进层级应该是最外面，就是和蓝色竖线那些内容是一个层级，也需要蓝色竖线，但是文字用灰色，文字字体和大小和目前那些灰色文字一样~~
36. ~~气泡内部的横线分割线应该比现在略短一点；另外每两步之间需要一行空行，每步内部不能空行，只能像之前一样用分割线；“已载入研究区：武汉市\_市 执行方案已确定，共 7 步”这一段和第一步之间也需要空行，前面说的“（本次为完全执行模式，默认不执行结果后处理；如需要无空洞的 10m 地表温度产品，告诉我即可对结果做空洞填补。）”这句话和上一步之间也需要空行~~
37. ~~“闭合校核完成：平均偏差 0.00 K，平均绝对误差 0.00 K，共比对 7,761,388 个格网（这是均值闭合校核，不是十米独立精度）”改成“闭合校核完成：平均偏差 0.00 K，平均绝对误差 0.00 K，共比对 7,761,388 个格网（此为均值闭合校核，并非10m地表温度产品精度，闭合误差约为0表明闭合校核通过）”~~
38. ~~气泡输出的数字不要用汉字，都要用阿拉伯数字，尤其是全流程的气泡输出需要注意（目前是汉字和阿拉伯数字混用的，既有10米也有十米这样的表述，都换成10m，单位也不要汉字，要全面排查类似的表述）~~
39. ~~"数据检查通过，进入模型训练"、"开始调优训练（第2轮）"、"训练测试差距略大，适度增加树数并降低叶节点样本以提升泛化"这几句依旧没有缩进（调优训练的每一轮都是这样没有缩进），我之前不是让你缩进了吗，而且是不带点的缩进，相当于和点对齐~~
40. ~~"模型训练（第一轮）完成：测试集决定系数 0.81，均方根误差 2.32 K"改为"模型训练（第1轮）完成：测试集决定系数 0.81，均方根误差 2.32 K"~~
41. ~~每一步之间是不需要分割线的，分割线的作用是区分每一步内部的不同内容，你现在第一步和第二步直接就有分割线，第二步和第三步之间也有~~
42. ~~"开始调优训练（第2轮）"改为"开始调优训练（总第2轮，调优第1轮）"后续以此类推~~
43. ~~不同步之间的空行要比现在略大一点，但是不要大太多（也包括开头和第一步之间的空行，以及最后结果解读和最后一句说明默认不执行结果后处理之间的空行），如果是规划出的步骤就包含了结果后处理时，就不需要结尾那句说明了~~
44. ~~有一个小bug，就是假如当前有一个项目正在运行，日志也会正常更新，当我再创建一个新项目或者是新对话时，该新项目对话的日志居然不是空的，好像会把正在运行的项目的日志的最新一句显示出来，过了一段时间或者是把这个对话删除再创建，新对话的日志才是空的~~
45. ~~现在在本地docker又莫名其妙出现了日志不更新、前端气泡光标没了，地图图层消失、文件列表中的文件被清空的情况，感觉又是这个正在运行的项目莫名其妙死了~~

